{ ... }:
let
    
in
{
    flake.modules.nixos.<name> = { ... }:
    let
        
    in
    {
        imports = [
        
        ];
                
        options = {
        
        };
    
        config = {
        
        };
    };
}